<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- إضافة المستخدمين للمجموعات -->
    <record id="user_admin" model="res.users">
        <field name="id" eval="ref('base.user_admin')"/>
        <field name="groups_id" eval="[(4, ref('group_estate_manager'))]"/>
    </record>
    
    <!-- إضافة جميع المستخدمين لمجموعة User -->
    <function 
        model="res.users" 
        name="add_to_group_all_users"
        eval="[ref('group_estate_user')]"
    />
</odoo>